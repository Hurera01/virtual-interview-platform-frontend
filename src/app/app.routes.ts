import { Routes } from '@angular/router';
import { InterviewComponent } from './component/interview/interview.component';

export const routes: Routes = [{ path: '', component: InterviewComponent }];
